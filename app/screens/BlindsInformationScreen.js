import React from "react";
import { Text, View, SafeAreaView, Button, Platform } from "react-native";

function BlindsInformationScreen({ navigation }) {
  return (
    <SafeAreaView>
      <Text>Blinds Information Screen</Text>
    </SafeAreaView>
  );
}

export default BlindsInformationScreen;
