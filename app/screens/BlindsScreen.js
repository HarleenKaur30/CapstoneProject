import React from "react";
import { Text, View, SafeAreaView, Button, Platform } from "react-native";
function BlindsScreen({ navigation }) {
  return (
    <SafeAreaView>
      <Text> Blinds Screen</Text>
    </SafeAreaView>
  );
}

export default BlindsScreen;
